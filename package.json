{
  "name": "zhipath-ai",
  "version": "0.1.0",
  "private": true,
  "description": "知途 AI 智能咨询 Agent 平台 monorepo",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r --if-present run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
